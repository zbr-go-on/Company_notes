# 自动货源发布系统 - 验收标准
## 总体验收标准
- 所有P0级任务全部完成
- 所有P0级测试用例100%通过
- 系统性能符合需求指标
- 代码符合团队规范
- 文档完整齐全

---

## 执行节奏验收（API闭环优先）
| 规则ID | 验收标准 |
| --- | --- |
| FLOW-ACC-1 | 必须按 `TASKS.md` 的“API闭环执行编排”推进，不得跳过闭环顺序 |
| FLOW-ACC-2 | 每个API闭环必须同时完成：业务逻辑、接口、异常处理、单元测试、Swagger验证 |
| FLOW-ACC-3 | 当前闭环未100%验收通过前，不得切换到下一个API闭环 |
| FLOW-ACC-4 | 不允许出现“集中开发业务逻辑后再集中开发API”的执行记录 |

## API闭环切换门禁（强制）

| 闭环ID | 通过门禁（必须全部满足） | 允许切换到 |
| --- | --- | --- |
| FLOW-0 | `T300-1~T300-5` 与 `T407-1~T407-4` 验收全部通过 | FLOW-1 |
| FLOW-1 | 单条发布链路相关任务验收通过：`T301/T302/T303/T304/T305-1/2/3/5/6/7/8/T401/T402/T404/T405-1/T406-1/2/3` | FLOW-2 |
| FLOW-2 | 批量发布链路相关任务验收通过：`T305-4/T403/T405-2/3/4/T406-4` | 集成测试阶段（T501+） |

> 说明：下方分阶段验收表按模块归档，不代表执行顺序；执行顺序以 `TASKS.md` 闭环编排为准。

---
## 第一阶段：项目初始化验收标准（已完成）
| 任务ID | 验收标准 |
| --- | --- |
| T001-1 | config/、src/、tests/、docs/四个一级目录创建成功 |
| T001-2 | src/下的api/、service/、dao/、clients/、utils/五个子目录创建成功 |
| T001-3 | 所有目录下的__init__.py文件存在 |
| T001-4 | 目录结构与ARCHITECTURE.md中的设计完全一致 |
| T002-1 | requirements.txt包含所有核心依赖 |
| T002-2 | requirements.txt包含所有工具依赖 |
| T002-3 | 执行`pip install -r requirements.txt`可以安装所有依赖无错误 |
| T003-1 | .env.example包含所有数据库配置项 |
| T003-2 | .env.example包含所有外部API配置项 |
| T003-3 | .env.example包含所有系统配置项，每个配置项有清晰中文注释 |
| T004-1 | Settings类定义了所有配置字段和正确的类型 |
| T004-2 | 多环境切换功能正常，根据ENV变量加载对应配置 |
| T004-3 | 可以正确读取config/default_values.yaml配置文件 |
| T004-4 | 环境变量优先级高于配置文件，读取功能正常 |
| T005-1 | default_values.yaml包含所有固定字段默认值 |
| T005-2 | default_values.yaml包含所有费用字段默认值 |
| T005-3 | default_values.yaml包含其他默认值，每个字段有注释说明 |
| T006-1 | 日志输出格式包含时间、级别、模块、函数、消息 |
| T006-2 | INFO及以上日志输出到控制台，ERROR及以上同时写入文件 |
| T006-3 | 日志文件按大小切割，保留最近7天的日志文件 |
| T007-1 | FastAPI应用实例创建成功，元信息配置正确 |
| T007-2 | GET /health接口返回正常，包含服务状态和版本信息 |
| T007-3 | CORS中间件配置正确，支持跨域请求，端口配置正确 |
| T008-1 | 依赖安装成功，无错误 |
| T008-2 | .env文件配置完成，包含所有必填配置项 |
| T008-3 | 服务启动成功，端口监听正常 |
| T008-4 | Swagger文档可以正常打开，/health接口调用正常 |

---

## 第二阶段：核心模块开发验收标准
### 🔹 数据访问层
| 任务ID | 验收标准 |
| --- | --- |
| T101-1 | SQLAlchemy相关模块导入正确 |
| T101-2 | 数据库引擎创建成功，连接池配置正确（max_overflow=10, pool_size=20） |
| T101-3 | SessionLocal会话工厂创建成功，get_db()依赖注入功能正常 |
| T101-4 | 数据库连接正常，可以执行简单查询 |
| T102-1 | Base基类和字段类型导入正确 |
| T102-2 | TransportOrder模型类创建成功 |
| T102-3 | 所有字段定义完整，与PRD中的transport_order表一致 |
| T102-4 | 每个字段有注释说明，主键、默认值、非空约束配置正确 |
| T102-5 | __repr__方法实现正确，调试打印信息清晰 |
| T102-6 | transport_order数据库表创建成功，字段与PRD完全一致 |
| T102-7 | LogisticsRouteInfo数据模型定义正确，字段与logistics_route_info表完全匹配（SQL建表由业务方处理） |

### 🔹 外部客户端层
| 任务ID | 验收标准 |
| --- | --- |
| T201-1 | generate_token函数可以正常生成Token |
| T201-2 | verify_token函数可以正确校验Token并返回payload，过期Token校验失败 |
| T201-3 | Token自动注入装饰器可以正常工作，外部API调用自动添加认证头 |
| T201-4 | Token功能单元测试100%通过 |
| T202-1 | AMapClient类创建成功，正常读取API_KEY配置 |
| T202-2 | geocode方法定义正确，接收地址文本参数 |
| T202-3 | 高德地图API调用正常，请求参数正确 |
| T202-4 | 返回结果解析正确，count=1时返回包含省、市、区、坐标的结构化信息 |
| T202-5 | 网络异常、API返回错误、多结果/无结果情况处理正确，抛出对应业务异常 |
| T202-6 | 请求重试机制正常，失败最多重试2次，每次间隔1秒 |
| T202-7 | AMapClient单元测试100%通过，覆盖所有场景 |
| T203-1 | FreightClient类创建成功，正常读取API_URL、API_TOKEN、DEFAULT_DEVICE_ID配置 |
| T203-2 | batch_query方法定义正确，device_id参数可选，默认使用配置值 |
| T203-3 | POST /routes/batch接口调用正常，自动注入Token认证头 |
| T203-4 | 返回结果解析正确，成功/失败情况处理正常 |
| T203-5 | 网络异常、API返回错误处理正确，记录详细日志 |
| T203-6 | FreightClient单元测试100%通过 |
| T204-1 | BusinessClient类创建成功，正常读取API_URL、API_TOKEN配置 |
| T204-2 | add_transport_order方法定义正确，接收订单数据参数 |
| T204-3 | POST /transport_order/add_transport_order接口调用正常，自动注入Token认证头 |
| T204-4 | 返回结果解析正确，成功/失败情况和错误码处理正常 |
| T204-5 | 网络异常、API返回错误处理正确，记录详细日志 |
| T204-6 | BusinessClient单元测试100%通过 |
| T205-1 | AMapClient单元测试覆盖所有场景：正常返回、多结果、无结果、网络异常 |
| T205-2 | FreightClient单元测试覆盖正常和异常场景 |
| T205-3 | BusinessClient单元测试覆盖正常和异常场景 |
| T205-4 | 所有外部客户端单元测试100%通过 |
| T205-5 | 单元测试覆盖率≥80% |

### 🔹 业务服务层
| 任务ID | 验收标准 |
| --- | --- |
| T300-1 | 可以正确连接 `wechat_core_data` 数据库并读取 `logistics_route_match_audit` 当前小时数据 |
| T300-2 | 数据过滤正确：`departure`、`destination`、`freight_cost` 任一为空的数据不会进入后续写入 |
| T300-3 | 过滤后的数据成功写入 `logistics_route_info`，字段映射正确且 `original_data` 有原始数据备份 |
| T300-4 | 同一 `route_id` 重复同步时执行更新，不产生重复记录（幂等） |
| T300-5 | 第0步单元测试100%通过，覆盖当前小时筛选、非空过滤、写入与更新场景 |
| T301-1 | AMapClient和相关异常类导入正确 |
| T301-2 | AddressParserService类创建成功，依赖注入AMapClient正常 |
| T301-3 | parse_address方法定义正确，接收地址文本参数 |
| T301-4 | AMapClient.geocode调用正常，获取解析结果 |
| T301-5 | 处理逻辑正确：count=1时返回结构化地址JSON，count≠1时记录警告日志，返回None |
| T301-6 | 结构化地址格式转换正确，符合transport_order表的start_address_json字段要求 |
| T301-7 | 地址清洗逻辑正常，去除特殊字符和多余空格 |
| T301-8 | AddressParserService单元测试100%通过，各种地址解析结果符合预期 |
| T302-1 | FreightClient和数据库模型导入正确 |
| T302-2 | FreightCalculatorService类创建成功，依赖注入FreightClient和数据库会话正常 |
| T302-3 | calculate_freight方法定义正确，调用运费计算API获取结果 |
| T302-4 | 结果保存逻辑正确，运费、距离、过路费等信息正确写入数据库logistics_route_info表 |
| T302-5 | FreightCalculatorService单元测试100%通过 |
| T303-1 | OrderValidatorService类创建成功 |
| T303-2 | validate_required_fields方法正确检查所有必填字段是否存在且非空 |
| T303-3 | validate_field_format方法正确检查字符串、数字、日期等格式正确性 |
| T303-4 | validate_address方法正确检查地址是否符合基本格式要求 |
| T303-5 | validate_numeric_range方法正确检查价格、数量等是否在合理范围内 |
| T303-6 | 校验错误信息封装正确，包含具体的错误字段和原因 |
| T303-7 | validate_batch方法正确遍历校验所有订单 |
| T303-8 | OrderValidatorService单元测试100%通过，覆盖所有校验规则 |
| T304-1 | OrderFillerService类创建成功，依赖注入配置对象正常 |
| T304-2 | fill_fixed_fields方法正确填充trade_type、store_id等固定值字段 |
| T304-3 | fill_default_fields方法正确从default_values.yaml读取默认值填充 |
| T304-4 | fill_system_fields方法正确填充创建人、创建时间、订单编号等系统字段 |
| T304-5 | fill_short_address方法正确从结构化地址中提取简称 |
| T304-6 | OrderFillerService单元测试100%通过，字段填充优先级正确（用户传入值>默认值） |
| T305-1 | 所有依赖服务导入正确 |
| T305-2 | OrderPublisherService类创建成功，依赖注入所有服务和数据库会话正常 |
| T305-3 | publish_single_order方法定义正确，处理单条订单发布全流程 |
| T305-4 | publish_batch_order方法定义正确，处理批量订单发布全流程 |
| T305-5 | 业务流程逻辑正确：地址解析→运费计算→字段填充→校验→写入数据库→调用业务API |
| T305-6 | 异常处理正确，每个步骤异常都记录详细日志，返回处理结果统计 |
| T305-7 | 幂等性判断逻辑正确，避免重复发布相同订单 |
| T305-8 | OrderPublisherService单元测试100%通过，覆盖正常流程和各种异常场景 |
| T306-1 | AddressParserService单元测试100%通过 |
| T307-1 | FreightCalculatorService单元测试100%通过 |
| T308-1 | OrderValidatorService和OrderFillerService单元测试100%通过 |
| T309-1 | OrderPublisherService单元测试100%通过 |
| T310-1 | 集成测试用例完整，覆盖完整的订单发布流程 |
| T310-2 | 集成测试100%通过，所有模块配合正常，流程正确 |
| T310-3 | 日志记录完整，数据库写入正确，外部API调用正常 |

---

## 第三阶段：API接口开发验收标准
| 任务ID | 验收标准 |
| --- | --- |
| T401-1 | 业务异常类定义完整，包含参数错误、业务错误、外部API错误等常用类型 |
| T401-2 | FastAPI全局异常处理器注册成功，能捕获所有类型的异常 |
| T401-3 | 统一返回格式包含code、message、data三个字段，格式规范 |
| T401-4 | 异常到返回格式的转换正确，错误码和错误信息映射清晰 |
| T401-5 | 异常处理功能正常，各种异常都能返回统一格式的错误响应 |
| T402-1 | 单条发布请求模型定义正确，包含所有必填字段和校验规则 |
| T402-2 | 单条发布响应模型定义正确，字段和类型符合要求 |
| T402-3 | 单条发布接口路由注册成功，路径为POST /transport_order/add |
| T402-4 | OrderPublisherService依赖注入正常，publish_single_order方法调用正确 |
| T402-5 | 返回结果包装为统一格式，状态码和信息正确 |
| T402-6 | 单条接口单元测试100%通过，覆盖正常和异常场景 |
| T403-1 | 批量发布请求模型定义正确，包含order_list数组，数组元素符合单条请求格式 |
| T403-2 | 批量发布响应模型定义正确，包含成功/失败/跳过统计和详情字段 |
| T403-3 | 批量发布接口路由注册成功，路径为POST /transport_order/batch_add |
| T403-4 | OrderPublisherService依赖注入正常，publish_batch_order方法调用正确 |
| T403-5 | 批量返回结果包装为统一格式，统计信息正确 |
| T403-6 | 批量接口单元测试100%通过，覆盖批量处理的各种场景 |
| T404-1 | Pydantic字段校验规则配置正确，包含必填、格式、范围等所有校验 |
| T404-2 | 校验错误信息为中文，清晰说明错误字段和原因 |
| T404-3 | 参数缺失、格式错误等场景的校验提示正确，返回清晰的错误信息 |
| T405-1 | 单条接口测试用例完整，覆盖所有参数校验和业务场景 |
| T405-2 | 批量接口测试用例完整，覆盖批量处理的各种场景 |
| T405-3 | 异常场景测试用例完整，覆盖所有可能的异常情况 |
| T405-4 | 所有接口单元测试100%通过 |
| T406-1 | Swagger接口说明完整，每个接口都有summary和description |
| T406-2 | Swagger文档包含参数示例和返回示例，清晰易懂 |
| T406-3 | 通过Swagger可以正常调用单条发布接口，功能符合预期 |
| T406-4 | 通过Swagger可以正常调用批量发布接口，功能符合预期 |
| T407-1 | 第0步同步接口路由注册成功，路径为 `POST /internal/route-sync/current-hour` |
| T407-2 | 接口可成功触发 `RouteSyncService.sync_current_hour`，并返回 `total_read/inserted/updated/skipped` |
| T407-3 | 接口支持 `source_table/target_table` 可选参数，默认正式表，支持固定测试表联调 |
| T407-4 | 第0步同步接口单元测试100%通过，覆盖成功和异常场景 |

---

## 第四阶段：集成测试验收标准
| 任务ID | 验收标准 |
| --- | --- |
| T501-1 | P0功能点梳理完整，没有遗漏 |
| T501-2 | 正常流程测试用例完整，覆盖单条和批量发布全流程 |
| T501-3 | 异常场景测试用例完整，覆盖地址解析、API调用、参数等所有异常场景 |
| T501-4 | 性能测试用例完整，包含响应时间、并发、批量处理速度等指标 |
| T502-1 | 测试环境部署完成，数据库和第三方API测试账号配置正确 |
| T502-2 | 单条发布全流程测试通过，从请求到发布成功所有环节正常 |
| T502-3 | 批量发布全流程测试通过，批量处理逻辑和统计结果正确 |
| T502-4 | 数据流转正确：数据库写入正确、业务API调用正确、日志记录完整 |
| T503-1 | 地址解析多结果/无结果场景处理正确，记录警告日志并跳过 |
| T503-2 | 运费计算API调用失败/超时场景处理正确，记录错误日志并返回失败 |
| T503-3 | 业务系统发布API调用失败/超时场景处理正确，记录错误日志并返回失败 |
| T503-4 | 参数缺失、格式错误、数值超限等场景校验正确，返回清晰错误信息 |
| T503-5 | 数据库异常场景处理正确，事务回滚，无脏数据写入 |
| T504-1 | 性能测试数据准备完成，包含100条正常订单数据 |
| T504-2 | 单条发布接口平均响应时间≤500ms |
| T504-3 | 地址解析平均响应时间≤200ms |
| T504-4 | 批量接口处理100条数据耗时≤30秒 |
| T504-5 | 100 QPS并发场景下无错误、无崩溃，成功率100% |
| T504-6 | 性能测试报告完整，记录所有测试指标和结果 |
| T505-1 | BUG清单完整，按P0/P1优先级分类清晰 |
| T505-2 | 所有P0级BUG全部修复，验证通过 |
| T505-3 | P1级BUG修复率≥95% |
| T506-1 | 所有功能测试用例100%通过 |
| T506-2 | 所有异常场景测试用例100%通过 |
| T506-3 | 修复BUG没有引入新的问题，所有原有功能正常 |

---

## 第五阶段：上线准备验收标准
| 任务ID | 验收标准 |
| --- | --- |
| T601-1 | Dockerfile编写正确，基于Python 3.10镜像，依赖安装和启动命令配置正确 |
| T601-2 | .dockerignore文件配置正确，排除不需要的日志、缓存、环境变量文件 |
| T601-3 | docker-compose.yml编写正确，包含服务、数据库、环境变量等完整配置 |
| T601-4 | Docker镜像构建成功，无错误 |
| T601-5 | docker-compose up可以一键启动所有服务，服务运行正常 |
| T602-1 | 部署环境要求清晰，包含硬件配置、软件版本、网络要求 |
| T602-2 | 部署步骤清晰可执行，任何人按照步骤都能完成部署 |
| T602-3 | 配置说明完整，所有环境变量和配置文件都有详细说明 |
| T602-4 | 常见问题排查指南完整，包含常见错误和解决方案 |
| T603-1 | API整体说明完整，包含功能介绍、使用流程、认证方式 |
| T603-2 | 单条发布接口文档完整，包含地址、请求参数、返回参数、示例 |
| T603-3 | 批量发布接口文档完整，包含地址、请求参数、返回参数、示例 |
| T603-4 | 错误码说明完整，所有可能的错误码和含义都有说明 |
| T604-1 | 项目介绍清晰，包含功能、优势、适用场景 |
| T604-2 | 快速启动指南清晰，本地开发环境搭建步骤详细可执行 |
| T604-3 | 配置说明完整，包含环境变量、配置文件、默认值说明 |
| T604-4 | 开发规范完整，包含目录结构、代码规范、测试要求 |
| T605-1 | 生产环境数据库配置正确，地址、账号、权限正常 |
| T605-2 | 生产环境API密钥配置正确，所有敏感信息通过环境变量管理，无明文泄露 |
| T605-3 | 生产环境日志配置正确，日志级别、存储路径、保留策略符合要求 |
| T605-4 | 生产环境服务启动正常，健康检查接口返回正常 |
| T606-1 | 灰度环境部署完成，流量策略配置正确 |
| T606-2 | 测试订单发布成功，全流程功能正常 |
| T606-3 | 数据库写入正确，业务系统正常接收到订单 |
| T606-4 | 灰度环境连续运行2小时无崩溃、无错误日志，稳定性达标 |


